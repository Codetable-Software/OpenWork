const {getConfig,setConfigValue,configPath}=require('../../config/manager');
function run(a){if(a[0]==='path')return console.log(configPath()); if(a[0]==='get'){const c=getConfig(); c.apiKey=c.apiKey?'[configured]':null; return console.log(JSON.stringify(c,null,2));} if(a[0]==='set'){const k=a[1],v=a.slice(2).join(' '); if(!k||!v)throw new Error('Usage: work config set <key> <value>'); const m={'api-key':'apiKey','model':'model','max-agent-steps':'maxAgentSteps','allow-shell':'allowShell','allow-network':'allowNetwork'}; if(!m[k])throw new Error(`Unknown setting: ${k}`); let x=v; if(k==='max-agent-steps'){x=Number(v);if(!Number.isInteger(x)||x<1||x>100)throw new Error('max-agent-steps must be 1-100');} if(k==='allow-shell'||k==='allow-network'){if(!/^(true|false)$/i.test(v))throw new Error(`${k} must be true or false`);x=v.toLowerCase()==='true';} setConfigValue(m[k],x); return console.log('Saved.');} console.log('Usage: work config get|path|set <key> <value>');}
module.exports={run};
